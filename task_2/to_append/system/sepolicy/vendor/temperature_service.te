type temperature_service_exec, exec_type, file_type, vendor_file_type;

allow temperature_service temperature_service_api:service_manager add;
allow temperature_service servicemanager:binder { call transfer };

init_daemon_domain(temperature_service)
